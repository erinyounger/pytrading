# xTrading 前端UI设计方案

## 🎨 设计概述

### 设计理念
**"专业金融，极致视觉"**

xTrading采用**专业金融交易终端美学**，结合**深色极简主义**与**鲜明数据可视化**，打造现代化量化交易桌面应用。

### 核心美学特征
- **深色交易终端风格**：深蓝/黑色主调，专业交易氛围
- **高对比度数据**：绿涨红跌，鲜明的数据层次
- **几何网格布局**：精确对齐，规整的金融数据展示
- **动态数据流**：实时更新的图表和指标卡
- **霓虹点缀**：蓝色/青色高亮，科技感十足

---

## 🎯 视觉风格

### 颜色方案

#### 主色调 (Dark Theme)
```css
:root {
  /* 主背景色 */
  --bg-primary: #0a0e1a;        /* 深蓝黑 */
  --bg-secondary: #131826;       /* 次级背景 */
  --bg-tertiary: #1a1f36;       /* 三级背景 */
  --bg-hover: #252b45;          /* 悬停背景 */

  /* 边框色 */
  --border-primary: #2a3550;     /* 主边框 */
  --border-secondary: #39456b;    /* 次级边框 */

  /* 文字色 */
  --text-primary: #e4e7ee;       /* 主文字 */
  --text-secondary: #9aa4bf;     /* 次级文字 */
  --text-tertiary: #6b7694;      /* 三级文字 */
  --text-disabled: #4a5568;      /* 禁用文字 */

  /* 功能色 */
  --success: #00d084;           /* 成功/涨 */
  --error: #ff4757;              /* 错误/跌 */
  --warning: #ffa502;             /* 警告 */
  --info: #00d4ff;               /* 信息/亮点 */

  /* 品牌色 */
  --brand-primary: #4f46e5;      /* 主品牌色 - 蓝紫 */
  --brand-secondary: #06b6d4;    /* 次品牌色 - 青色 */
  --brand-accent: #8b5cf6;       /* 强调色 - 紫色 */
}
```

#### Light Theme (支持切换)
```css
:root[data-theme="light"] {
  --bg-primary: #ffffff;
  --bg-secondary: #f8fafc;
  --bg-tertiary: #f1f5f9;
  --bg-hover: #e2e8f0;

  --text-primary: #1e293b;
  --text-secondary: #475569;
  --text-tertiary: #64748b;
}
```

### 字体方案

#### 中文字体
- **主字体**: "PingFang SC", "Microsoft YaHei", "微软雅黑"
- **代码字体**: "JetBrains Mono", "Fira Code", "Consolas"

#### 英文字体
- **主字体**: "Inter", "SF Pro Display" (系统字体)
- **标题字体**: "Space Grotesk", "Montserrat"
- **数据字体**: "JetBrains Mono", "Roboto Mono"

#### 字体层级
```css
/* 标题字体 */
.text-4xl { font-family: 'Space Grotesk', sans-serif; font-weight: 700; }
.text-3xl { font-family: 'Space Grotesk', sans-serif; font-weight: 600; }
.text-2xl { font-family: 'Space Grotesk', sans-serif; font-weight: 600; }
.text-xl { font-family: 'Space Grotesk', sans-serif; font-weight: 500; }

/* 正文字体 */
.text-base { font-family: 'Inter', sans-serif; font-weight: 400; }
.text-sm { font-family: 'Inter', sans-serif; font-weight: 400; }
.text-xs { font-family: 'Inter', sans-serif; font-weight: 400; }

/* 数字/代码字体 */
.font-mono { font-family: 'JetBrains Mono', monospace; }
```

---

## 📐 整体布局设计

### 布局结构

```
┌─────────────────────────────────────────────────────┐
│                    顶部标题栏                          │
│  xTrading Logo  |  标题  |  用户 | 设置 | 最小化  │
├───────────────┬───────────────────────────────────────┤
│               │                                       │
│               │                                       │
│    左侧导航    │              主内容区域                │
│     侧边栏      │                                     │
│               │         (动态切换页面内容)             │
│               │                                     │
│               │                                       │
│               │                                       │
├───────────────┴───────────────────────────────────────┤
│                    底部状态栏                          │
│  连接状态  |  数据更新时间  |  性能监控  |  快捷操作  │
└─────────────────────────────────────────────────────┘
```

### 布局规格

#### 侧边导航栏
- **宽度**: 280px (固定)
- **背景**: `var(--bg-secondary)`
- **边框**: 右侧 1px solid `var(--border-primary)`
- **padding**: 24px

#### 主内容区
- **左边距**: 280px (侧边栏宽度)
- **padding**: 32px
- **最小宽度**: 1024px (确保布局稳定)

#### 顶部标题栏
- **高度**: 64px
- **背景**: `var(--bg-primary)`
- **边框**: 底部 1px solid `var(--border-primary)`
- **padding**: 0 24px

#### 底部状态栏
- **高度**: 48px
- **背景**: `var(--bg-secondary)`
- **边框**: 顶部 1px solid `var(--border-primary)`
- **padding**: 0 24px

---

## 🧭 导航设计

### 左侧导航栏结构

```
xTrading
  ├─ 🏠 仪表板
  ├─ 📊 实时行情
  │    ├─ 行情监控
  │    ├─ 涨跌幅排行
  │    └─ K线图表
  ├─ 🔄 回测管理
  │    ├─ 创建回测
  │    ├─ 历史回测
  │    └─ 回测结果
  ├─ ⚙️ 策略管理
  │    ├─ 我的策略
  │    ├─ 策略编辑
  │    └─ 策略回测
  ├─ 📡 交易信号
  │    ├─ 信号列表
  │    ├─ 信号详情
  │    └─ 信号过滤
  ├─ 🛡️ 风险管理
  │    ├─ 风险指标
  │    ├─ 预警设置
  │    └─ 止损止盈
  └─ 📈 性能报告
       ├─ 绩效分析
       ├─ 交易明细
       └─ 收益曲线
```

### 导航样式

```css
.nav-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 12px 16px;
  margin: 4px 8px;
  border-radius: 8px;
  color: var(--text-secondary);
  text-decoration: none;
  transition: all 0.2s ease;
  font-family: 'Inter', sans-serif;
  font-weight: 500;
}

.nav-item:hover {
  background: var(--bg-hover);
  color: var(--text-primary);
  transform: translateX(4px);
}

.nav-item.active {
  background: linear-gradient(135deg, var(--brand-primary), var(--brand-accent));
  color: white;
  box-shadow: 0 4px 20px rgba(79, 70, 229, 0.3);
}

.nav-item.active::before {
  content: '';
  position: absolute;
  left: -24px;
  width: 4px;
  height: 100%;
  background: var(--brand-secondary);
  border-radius: 0 2px 2px 0;
}
```

---

## 📱 页面详细设计

### 1. 仪表板 (Dashboard)

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│                        欢迎栏                                │
│              早上好，xTrader | 今日概览                      │
├───────────────┬─────────────────┬───────────────────────────┤
│   资产总览    │    今日收益      │      活跃策略              │
│   ¥1,234,567 │   +¥12,345      │        5个                │
│   (+2.45%)   │   (+1.23%)      │                          │
├───────────────┴─────────────────┴───────────────────────────┤
│                       收益曲线图                              │
│         [大型面积图 - 展示7天收益走势]                        │
├─────────────────┬─────────────────┬─────────────────────────┤
│    持仓分布     │    交易统计     │      最近交易              │
│   [饼图显示]    │   [柱状图显示]  │   [表格显示]              │
│                 │                 │                         │
└─────────────────┴─────────────────┴─────────────────────────┘
```

#### 核心组件

**指标卡片 (MetricCard)**
```tsx
<div className="metric-card">
  <div className="metric-icon">
    <TrendingUp className="w-6 h-6 text-green-400" />
  </div>
  <div className="metric-content">
    <div className="metric-label">总资产</div>
    <div className="metric-value">¥1,234,567</div>
    <div className="metric-change positive">+2.45%</div>
  </div>
  <div className="metric-chart">
    <MiniAreaChart data={data} />
  </div>
</div>
```

**收益曲线 (ReturnChart)**
```tsx
<div className="return-chart-container">
  <div className="chart-header">
    <h3>收益曲线</h3>
    <div className="time-selector">
      <button>1天</button>
      <button>7天</button>
      <button className="active">30天</button>
      <button>1年</button>
    </div>
  </div>
  <div className="chart-content">
    <AreaChart data={returnData}>
      <defs>
        <linearGradient id="returnGradient" x1="0" y1="0" x2="0" y2="1">
          <stop offset="5%" stopColor="#4f46e5" stopOpacity={0.3}/>
          <stop offset="95%" stopColor="#4f46e5" stopOpacity={0}/>
        </linearGradient>
      </defs>
      <Area
        type="monotone"
        dataKey="value"
        stroke="#4f46e5"
        strokeWidth={2}
        fillOpacity={1}
        fill="url(#returnGradient)"
      />
    </AreaChart>
  </div>
</div>
```

### 2. 实时行情 (Market Data)

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│                      行情搜索栏                               │
│  [搜索框: 输入股票代码] [实时开关] [刷新频率设置]              │
├─────────────────────────────────────────────────────────────┤
│                        指数行情                              │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐   │
│  │  上证指数   │   深证成指   │   创业板指   │   科创50    │   │
│  │  3,245.67  │  12,345.89  │  2,456.78   │  1,234.56  │   │
│  │  (+1.23%)  │  (+0.87%)   │  (+2.34%)   │  (-0.56%)  │   │
│  └─────────────┴─────────────┴─────────────┴─────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                      K线图表区                               │
│              [TradingView图表或自定义K线图]                   │
├───────────────┬───────────────────────────────────────────────┤
│   涨跌幅排行  │                  成交明细                      │
│  ┌─────────┐  │  ┌─────────────────────────────────────────┐  │
│  │股票代码  │  │  │时间    │方向│价格   │数量   │金额      │  │
│  │涨幅%    │  │  │10:30:25│买  │123.45│100    │12,345   │  │
│  │成交量   │  │  │10:30:24│卖  │123.40│200    │24,680   │  │
│  └─────────┘  │  └─────────────────────────────────────────┘  │
│               │                                             │
│  [排序表格]   │            [实时滚动表格]                     │
└───────────────┴───────────────────────────────────────────────┘
```

#### 核心组件

**行情卡片 (MarketCard)**
```tsx
<div className="market-card">
  <div className="market-header">
    <span className="market-name">{name}</span>
    <span className="market-status online">实时</span>
  </div>
  <div className="market-price">
    <span className="price">{price}</span>
    <span className={`change ${changeType}`}>
      {change} ({changePercent}%)
    </span>
  </div>
  <div className="market-volume">
    成交量: {volume}
  </div>
</div>
```

**涨跌幅表格 (RankingTable)**
```tsx
<div className="ranking-table">
  <table>
    <thead>
      <tr>
        <th>排名</th>
        <th>股票代码</th>
        <th>股票名称</th>
        <th>现价</th>
        <th>涨跌额</th>
        <th>涨跌幅</th>
        <th>成交量</th>
      </tr>
    </thead>
    <tbody>
      {data.map((item, index) => (
        <tr key={item.symbol} className="ranking-row">
          <td className="rank">{index + 1}</td>
          <td className="symbol">{item.symbol}</td>
          <td className="name">{item.name}</td>
          <td className="price">{item.price}</td>
          <td className={`change-amount ${item.changeType}`}>
            {item.changeAmount}
          </td>
          <td className={`change-percent ${item.changeType}`}>
            {item.changePercent}%
          </td>
          <td className="volume">{item.volume}</td>
        </tr>
      ))}
    </tbody>
  </table>
</div>
```

### 3. 回测管理 (Backtest Management)

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│                      快速创建                               │
│  [策略选择] [股票池] [时间范围] [创建回测按钮]              │
├─────────────────────────────────────────────────────────────┤
│                     回测任务列表                             │
│  ┌───────────────────────────────────────────────────────┐ │
│  │任务ID: BT_20241201_001    状态: 运行中    进度: 65%    │ │
│  │策略: MACD策略    股票数: 300    开始时间: 09:30       │ │
│  │预计完成: 10:45    [查看日志] [暂停] [删除]             │ │
│  └───────────────────────────────────────────────────────┘ │
│  ┌───────────────────────────────────────────────────────┐ │
│  │任务ID: BT_20241201_002    状态: 已完成    耗时: 12m   │ │
│  │策略: 布林带策略  股票数: 100    开始时间: 09:15       │ │
│  │收益率: +15.67%  夏普比率: 1.23  [查看结果] [重新回测] │ │
│  └───────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│                     回测结果概览                             │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐ │
│  │  总收益率   │  年化收益   │  最大回撤   │  夏普比率   │ │
│  │  +15.67%    │  +23.45%    │  -8.23%     │   1.23      │ │
│  └─────────────┴─────────────┴─────────────┴─────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

#### 核心组件

**回测任务卡片 (BacktestTaskCard)**
```tsx
<div className="backtest-task-card">
  <div className="task-header">
    <div className="task-id">{task.id}</div>
    <div className={`task-status ${task.status}`}>
      {task.statusText}
    </div>
  </div>

  <div className="task-info">
    <div className="info-row">
      <span className="label">策略:</span>
      <span className="value">{task.strategy}</span>
    </div>
    <div className="info-row">
      <span className="label">股票数:</span>
      <span className="value">{task.stockCount}</span>
    </div>
    <div className="info-row">
      <span className="label">开始时间:</span>
      <span className="value">{task.startTime}</span>
    </div>
  </div>

  {task.status === 'running' && (
    <div className="progress-section">
      <div className="progress-bar">
        <div
          className="progress-fill"
          style={{ width: `${task.progress}%` }}
        />
      </div>
      <div className="progress-text">
        进度: {task.progress}% | 预计完成: {task.eta}
      </div>
    </div>
  )}

  {task.status === 'completed' && (
    <div className="result-summary">
      <div className="result-item">
        <span className="label">收益率:</span>
        <span className="value positive">{task.returnRate}</span>
      </div>
      <div className="result-item">
        <span className="label">夏普比率:</span>
        <span className="value">{task.sharpeRatio}</span>
      </div>
    </div>
  )}

  <div className="task-actions">
    {task.status === 'running' && (
      <>
        <button className="btn-secondary">查看日志</button>
        <button className="btn-warning">暂停</button>
      </>
    )}
    {task.status === 'completed' && (
      <>
        <button className="btn-primary">查看结果</button>
        <button className="btn-secondary">重新回测</button>
      </>
    )}
    <button className="btn-danger">删除</button>
  </div>
</div>
```

### 4. 策略管理 (Strategy Management)

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│                    策略操作栏                                │
│  [新建策略] [导入策略] [导出策略] [策略模板] [批量操作]       │
├─────────────────────────────────────────────────────────────┤
│                     策略列表                                │
│  ┌───────────┬──────────┬──────────┬──────────┬──────────┐  │
│  │ ☑ 策略名  │  策略类型 │  运行状态 │  今日收益 │  操作     │  │
│  │ MACD策略  │  趋势跟踪 │  运行中   │  +2.34%  │ 编辑/暂停 │  │
│  │ 布林带策略│  均值回归 │  已暂停   │  -1.23%  │ 编辑/启动 │  │
│  │ 海龟策略  │  突破策略 │  运行中   │  +0.87%  │ 编辑/暂停 │  │
│  └───────────┴──────────┴──────────┴──────────┴──────────┘  │
├─────────────────────────────────────────────────────────────┤
│                      策略编辑器                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ 策略配置: MACD参数设置                                │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ fast_period: [12]  slow_period: [26]                 │  │
│  │ signal_period: [9]  position_size: [10%]             │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ 风险控制:                                            │  │
│  │ stop_loss: [5%]  take_profit: [10%]  max_position: [20%] │ │
│  ├───────────────────────────────────────────────────────┤  │
│  │ [保存配置] [回测验证] [启动策略] [重置为默认]          │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

### 5. 交易信号 (Trading Signals)

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│                    信号筛选器                                │
│  [策略类型] [信号类型] [时间范围] [强度筛选] [搜索框]        │
├─────────────────────────────────────────────────────────────┤
│                      信号列表                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ [🟢强买] 浦发银行(600000)  10:30:25  强度: 0.85     │  │
│  │ 策略: MACD金叉  价格: 8.95  成交量: 2.3万手         │  │
│  │ 理由: MACD指标出现金叉信号，短期上涨概率较高           │  │
│  │ [加入自选] [查看详情] [模拟买入]                      │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ [🔴强卖] 万科A(000002)   10:28:12  强度: 0.92     │  │
│  │ 策略: 布林带跌破  价格: 15.60 成交量: 1.8万手        │  │
│  │ 理由: 价格跌破布林带下轨，建议减仓或清仓              │  │
│  │ [加入黑名单] [查看详情] [模拟卖出]                    │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                    信号统计概览                              │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐ │
│  │ 今日信号数  │ 买入信号    │ 卖出信号    │ 准确率      │ │
│  │    156     │     89      │     67      │   73.5%     │ │
│  └─────────────┴─────────────┴─────────────┴─────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 6. 风险管理 (Risk Management)

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│                     风险总览                                │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐  │
│  │ 组合风险度  │  今日最大回撤│  持仓集中度 │  杠杆比率   │  │
│  │    中等     │    -2.34%   │    35%     │    1.2x    │  │
│  └─────────────┴─────────────┴─────────────┴─────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                      风险预警                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │ ⚠️  持仓集中度预警                                    │  │
│  │ 单只股票持仓超过30%，建议分散投资                      │  │
│  │ 时间: 10:25:30  级别: 中等  [查看详情] [处理]         │  │
│  ├───────────────────────────────────────────────────────┤  │
│  │ 🔴 止损预警                                          │  │
│  │ 招商银行(600036)触发止损条件，当前亏损5.2%            │  │
│  │ 时间: 10:20:15  级别: 高  [立即处理] [调整参数]       │  │
│  └───────────────────────────────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│                      风险指标图表                            │
│           [风险敞口分布图] [回撤曲线图] [相关性热力图]       │
└─────────────────────────────────────────────────────────────┘
```

### 7. 性能报告 (Performance Reports)

#### 布局结构
```
┌─────────────────────────────────────────────────────────────┐
│                    报告筛选器                                │
│  [时间范围] [策略选择] [股票池] [导出PDF] [导出Excel]      │
├─────────────────────────────────────────────────────────────┤
│                     核心指标                                │
│  ┌─────────────┬─────────────┬─────────────┬─────────────┐ │
│  │  总收益率   │  年化收益   │  夏普比率   │  最大回撤   │ │
│  │  +23.45%    │  +28.67%   │   1.85     │   -12.34%  │ │
│  └─────────────┴─────────────┴─────────────┴─────────────┘ │
├─────────────────┬─────────────────┬─────────────────────────┤
│    收益曲线     │    月度收益     │        交易统计         │
│  [面积图显示]   │   [柱状图显示]  │   ┌─────┬─────┬─────┐    │
│                 │                 │   │胜率  │盈亏比│次数 │    │
│                 │                 │   │65%  │1.8  │234  │    │
│                 │                 │   └─────┴─────┴─────┘    │
├─────────────────┴─────────────────┴─────────────────────────┤
│                      交易明细                               │
│  ┌───────────────────────────────────────────────────────┐  │
│  │日期      │股票    │方向│价格   │数量   │手续费│净收益 │  │
│  │12-01     │600000  │买  │8.95   │10000  │5.2   │+234  │  │
│  │11-30     │000002  │卖  │15.60  │5000   │3.8   │-156  │  │
│  └───────────────────────────────────────────────────────┘  │
│                           [加载更多]                        │
└─────────────────────────────────────────────────────────────┘
```

---

## 🧩 组件设计

### 基础组件

#### 1. 指标卡片 (MetricCard)
```tsx
interface MetricCardProps {
  title: string;
  value: string | number;
  change?: string;
  changeType?: 'positive' | 'negative' | 'neutral';
  icon?: React.ReactNode;
  trend?: 'up' | 'down' | 'flat';
  data?: number[];
}

export const MetricCard: React.FC<MetricCardProps> = ({
  title,
  value,
  change,
  changeType = 'neutral',
  icon,
  trend,
  data
}) => {
  return (
    <div className="metric-card">
      <div className="metric-header">
        <span className="metric-title">{title}</span>
        {icon && <div className="metric-icon">{icon}</div>}
      </div>
      <div className="metric-value">{value}</div>
      {change && (
        <div className={`metric-change ${changeType}`}>
          {trend && <span className={`trend-icon ${trend}`} />}
          {change}
        </div>
      )}
      {data && <MiniChart data={data} trend={trend} />}
    </div>
  );
};
```

#### 2. 数据表格 (DataTable)
```tsx
interface Column {
  key: string;
  title: string;
  width?: string;
  render?: (value: any, record: any) => React.ReactNode;
  sortable?: boolean;
}

interface DataTableProps {
  columns: Column[];
  data: any[];
  loading?: boolean;
  pagination?: boolean;
  onSort?: (key: string, order: 'asc' | 'desc') => void;
}

export const DataTable: React.FC<DataTableProps> = ({
  columns,
  data,
  loading,
  pagination,
  onSort
}) => {
  return (
    <div className="data-table">
      <table>
        <thead>
          <tr>
            {columns.map((column) => (
              <th
                key={column.key}
                style={{ width: column.width }}
                className={column.sortable ? 'sortable' : ''}
                onClick={() => column.sortable && onSort?.(column.key, 'asc')}
              >
                {column.title}
                {column.sortable && <SortIcon />}
              </th>
            ))}
          </tr>
        </thead>
        <tbody>
          {loading ? (
            <tr><td colSpan={columns.length} className="loading">加载中...</td></tr>
          ) : (
            data.map((record, index) => (
              <tr key={index}>
                {columns.map((column) => (
                  <td key={column.key}>
                    {column.render
                      ? column.render(record[column.key], record)
                      : record[column.key]}
                  </td>
                ))}
              </tr>
            ))
          )}
        </tbody>
      </table>
      {pagination && <TablePagination />}
    </div>
  );
};
```

#### 3. 图表容器 (ChartContainer)
```tsx
interface ChartContainerProps {
  title: string;
  subtitle?: string;
  children: React.ReactNode;
  actions?: React.ReactNode;
  loading?: boolean;
  error?: string;
}

export const ChartContainer: React.FC<ChartContainerProps> = ({
  title,
  subtitle,
  children,
  actions,
  loading,
  error
}) => {
  return (
    <div className="chart-container">
      <div className="chart-header">
        <div className="chart-title">
          <h3>{title}</h3>
          {subtitle && <span className="chart-subtitle">{subtitle}</span>}
        </div>
        {actions && <div className="chart-actions">{actions}</div>}
      </div>
      <div className="chart-content">
        {loading ? (
          <div className="chart-loading">加载中...</div>
        ) : error ? (
          <div className="chart-error">{error}</div>
        ) : (
          children
        )}
      </div>
    </div>
  );
};
```

#### 4. 状态标签 (StatusTag)
```tsx
type StatusType = 'success' | 'error' | 'warning' | 'info' | 'running' | 'paused';

interface StatusTagProps {
  status: StatusType;
  children: React.ReactNode;
}

export const StatusTag: React.FC<StatusTagProps> = ({ status, children }) => {
  return (
    <span className={`status-tag ${status}`}>
      <span className="status-dot" />
      {children}
    </span>
  );
};
```

### 复合组件

#### 1. 策略卡片 (StrategyCard)
```tsx
export const StrategyCard: React.FC<StrategyCardProps> = ({ strategy }) => {
  return (
    <div className="strategy-card">
      <div className="strategy-header">
        <h3 className="strategy-name">{strategy.name}</h3>
        <StatusTag status={strategy.status}>
          {strategy.statusText}
        </StatusTag>
      </div>

      <div className="strategy-info">
        <div className="info-item">
          <span className="label">类型:</span>
          <span className="value">{strategy.type}</span>
        </div>
        <div className="info-item">
          <span className="label">股票数:</span>
          <span className="value">{strategy.stockCount}</span>
        </div>
      </div>

      <div className="strategy-metrics">
        <MetricCard
          title="今日收益"
          value={strategy.todayReturn}
          changeType={strategy.returnType}
        />
        <MetricCard
          title="总收益"
          value={strategy.totalReturn}
          changeType={strategy.totalReturnType}
        />
      </div>

      <div className="strategy-actions">
        <button className="btn-primary">编辑</button>
        <button className="btn-secondary">
          {strategy.status === 'running' ? '暂停' : '启动'}
        </button>
        <button className="btn-danger">删除</button>
      </div>
    </div>
  );
};
```

#### 2. 回测结果卡片 (BacktestResultCard)
```tsx
export const BacktestResultCard: React.FC<BacktestResultCardProps> = ({ result }) => {
  return (
    <div className="backtest-result-card">
      <div className="result-header">
        <h4>{result.name}</h4>
        <span className="result-date">{result.date}</span>
      </div>

      <div className="result-metrics">
        <div className="metric-grid">
          <div className="metric-item">
            <span className="metric-label">总收益</span>
            <span className={`metric-value ${result.returnType}`}>
              {result.totalReturn}
            </span>
          </div>
          <div className="metric-item">
            <span className="metric-label">夏普比率</span>
            <span className="metric-value">{result.sharpeRatio}</span>
          </div>
          <div className="metric-item">
            <span className="metric-label">最大回撤</span>
            <span className="metric-value negative">{result.maxDrawdown}</span>
          </div>
          <div className="metric-item">
            <span className="metric-label">胜率</span>
            <span className="metric-value">{result.winRate}</span>
          </div>
        </div>
      </div>

      <div className="result-chart">
        <ReturnChart data={result.returnData} />
      </div>

      <div className="result-actions">
        <button className="btn-primary">查看详情</button>
        <button className="btn-secondary">重新回测</button>
        <button className="btn-outline">导出报告</button>
      </div>
    </div>
  );
};
```

---

## 🎨 动画与交互设计

### 微动画

#### 1. 页面切换动画
```css
/* 页面切换淡入效果 */
.page-enter {
  opacity: 0;
  transform: translateY(20px);
}

.page-enter-active {
  opacity: 1;
  transform: translateY(0);
  transition: opacity 0.3s ease, transform 0.3s ease;
}

/* 卡片悬停动画 */
.card {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

.card:hover {
  transform: translateY(-4px);
  box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
}

/* 按钮点击反馈 */
.btn {
  transition: all 0.2s ease;
}

.btn:active {
  transform: scale(0.98);
}

/* 数据更新动画 */
.data-update {
  animation: dataFlash 1s ease-in-out;
}

@keyframes dataFlash {
  0%, 100% { background-color: transparent; }
  50% { background-color: rgba(79, 70, 229, 0.2); }
}
```

#### 2. 加载动画
```css
/* 脉冲加载效果 */
.loading-pulse {
  animation: pulse 2s cubic-bezier(0.4, 0, 0.6, 1) infinite;
}

@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* 旋转加载 */
.loading-spinner {
  animation: spin 1s linear infinite;
}

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}

/* 骨架屏动画 */
.skeleton {
  background: linear-gradient(
    90deg,
    var(--bg-tertiary) 25%,
    var(--bg-hover) 50%,
    var(--bg-tertiary) 75%
  );
  background-size: 200% 100%;
  animation: skeleton-loading 1.5s infinite;
}

@keyframes skeleton-loading {
  0% { background-position: 200% 0; }
  100% { background-position: -200% 0; }
}
```

#### 3. 数据变化动画
```css
/* 数字滚动动画 */
@keyframes numberRoll {
  from { transform: translateY(-10px); opacity: 0; }
  to { transform: translateY(0); opacity: 1; }
}

.number-change {
  animation: numberRoll 0.5s ease-out;
}

/* 上涨下跌动画 */
.price-up {
  animation: flashGreen 1s ease-in-out;
}

.price-down {
  animation: flashRed 1s ease-in-out;
}

@keyframes flashGreen {
  0%, 100% { color: var(--text-primary); }
  50% { color: var(--success); }
}

@keyframes flashRed {
  0%, 100% { color: var(--text-primary); }
  50% { color: var(--error); }
}
```

### 交互效果

#### 1. 表格交互
```tsx
// 表格行悬停高亮
const TableRow = ({ record, onClick }) => {
  return (
    <tr
      className="table-row"
      onClick={() => onClick(record)}
      onMouseEnter={() => setHovered(true)}
      onMouseLeave={() => setHovered(false)}
    >
      {/* 单元格内容 */}
    </tr>
  );
};

// 排序交互
const handleSort = (key: string, order: 'asc' | 'desc') => {
  setSortKey(key);
  setSortOrder(order);
  // 排序逻辑
};
```

#### 2. 图表交互
```tsx
// 图表悬停提示
const CustomTooltip = ({ active, payload, label }) => {
  if (active && payload && payload.length) {
    return (
      <div className="custom-tooltip">
        <p className="tooltip-label">{label}</p>
        {payload.map((entry, index) => (
          <p key={index} className="tooltip-value" style={{ color: entry.color }}>
            {entry.name}: {entry.value}
          </p>
        ))}
      </div>
    );
  }
  return null;
};

// 图表点击事件
const onChartClick = (data: any) => {
  // 处理点击事件
  navigate(`/detail/${data.symbol}`);
};
```

---

## 📱 响应式适配

### 断点设置
```css
/* Tailwind CSS 断点 */
--screen-sm: 640px;
--screen-md: 768px;
--screen-lg: 1024px;
--screen-xl: 1280px;
--screen-2xl: 1536px;
```

### 布局适配

#### 桌面端 (1280px+)
```css
.desktop-layout {
  display: grid;
  grid-template-columns: 280px 1fr;
  grid-template-rows: 64px 1fr 48px;
  min-height: 100vh;
}
```

#### 平板端 (768px - 1279px)
```css
.tablet-layout {
  grid-template-columns: 1fr;
  grid-template-rows: auto 1fr auto;
}

.tablet-layout .sidebar {
  position: fixed;
  left: -280px;
  top: 0;
  height: 100vh;
  z-index: 100;
  transition: left 0.3s ease;
}

.tablet-layout .sidebar.open {
  left: 0;
}
```

#### 移动端 (< 768px)
```css
.mobile-layout {
  grid-template-columns: 1fr;
  grid-template-rows: 56px 1fr 56px;
}

.mobile-layout .content {
  padding: 16px;
}

.mobile-layout .metric-card {
  margin-bottom: 16px;
}
```

### 组件适配

#### 导航栏
```tsx
const Navigation = () => {
  const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false);

  return (
    <>
      {/* 桌面端导航 */}
      <nav className="hidden md:block">
        <div className="nav-item">仪表板</div>
        <div className="nav-item">行情</div>
        {/* ... */}
      </nav>

      {/* 移动端菜单按钮 */}
      <button
        className="md:hidden mobile-menu-button"
        onClick={() => setIsMobileMenuOpen(!isMobileMenuOpen)}
      >
        <MenuIcon />
      </button>

      {/* 移动端侧边栏 */}
      <div className={`mobile-sidebar ${isMobileMenuOpen ? 'open' : ''}`}>
        <div className="nav-item">仪表板</div>
        <div className="nav-item">行情</div>
        {/* ... */}
      </div>
    </>
  );
};
```

#### 表格响应式
```tsx
const ResponsiveTable = ({ columns, data }) => {
  return (
    <div className="responsive-table">
      {/* 桌面端表格 */}
      <table className="hidden md:table">
        <thead>
          <tr>
            {columns.map(col => <th key={col.key}>{col.title}</th>)}
          </tr>
        </thead>
        <tbody>
          {data.map((record, index) => (
            <tr key={index}>
              {columns.map(col => (
                <td key={col.key}>{record[col.key]}</td>
              ))}
            </tr>
          ))}
        </tbody>
      </table>

      {/* 移动端卡片 */}
      <div className="md:hidden card-list">
        {data.map((record, index) => (
          <div key={index} className="record-card">
            {columns.map(col => (
              <div key={col.key} className="record-field">
                <span className="field-label">{col.title}:</span>
                <span className="field-value">{record[col.key]}</span>
              </div>
            ))}
          </div>
        ))}
      </div>
    </div>
  );
};
```

---

## 🎯 设计系统规范

### 间距系统
```css
:root {
  /* 4px 基础单位 */
  --space-1: 0.25rem;  /* 4px */
  --space-2: 0.5rem;   /* 8px */
  --space-3: 0.75rem;  /* 12px */
  --space-4: 1rem;     /* 16px */
  --space-5: 1.25rem;  /* 20px */
  --space-6: 1.5rem;   /* 24px */
  --space-8: 2rem;     /* 32px */
  --space-10: 2.5rem;  /* 40px */
  --space-12: 3rem;    /* 48px */
  --space-16: 4rem;    /* 64px */
  --space-20: 5rem;    /* 80px */
}
```

### 圆角系统
```css
:root {
  --radius-none: 0;
  --radius-sm: 0.125rem;   /* 2px */
  --radius-base: 0.25rem;   /* 4px */
  --radius-md: 0.375rem;    /* 6px */
  --radius-lg: 0.5rem;      /* 8px */
  --radius-xl: 0.75rem;     /* 12px */
  --radius-2xl: 1rem;       /* 16px */
  --radius-full: 9999px;
}
```

### 阴影系统
```css
:root {
  --shadow-sm: 0 1px 2px 0 rgba(0, 0, 0, 0.05);
  --shadow-base: 0 1px 3px 0 rgba(0, 0, 0, 0.1), 0 1px 2px 0 rgba(0, 0, 0, 0.06);
  --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
  --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 0 4px 6px -2px rgba(0, 0, 0, 0.05);
  --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1), 0 10px 10px -5px rgba(0, 0, 0, 0.04);
  --shadow-glow: 0 0 20px rgba(79, 70, 229, 0.3);
}
```

### 过渡动画
```css
:root {
  --transition-fast: 150ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-base: 300ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-slow: 500ms cubic-bezier(0.4, 0, 0.2, 1);
}
```

---

## 🚀 实现建议

### 1. 开发优先级

**Phase 1: 基础框架 (Week 1-2)**
- [ ] 搭建 React + TypeScript + Tailwind 项目
- [ ] 集成 shadcn/ui 组件库
- [ ] 实现主题切换功能
- [ ] 创建基础布局组件
- [ ] 实现路由系统

**Phase 2: 核心页面 (Week 3-4)**
- [ ] 仪表板页面开发
- [ ] 实时行情页面开发
- [ ] 回测管理页面开发
- [ ] 基础图表组件封装

**Phase 3: 高级功能 (Week 5-6)**
- [ ] 策略管理页面开发
- [ ] 交易信号页面开发
- [ ] 风险管理页面开发
- [ ] 性能报告页面开发

**Phase 4: 优化与完善 (Week 7-8)**
- [ ] 响应式适配优化
- [ ] 动画效果完善
- [ ] 性能优化
- [ ] 用户体验优化

### 2. 技术实现要点

#### 状态管理
```tsx
// 使用 Zustand 管理全局状态
import { create } from 'zustand';

interface AppState {
  theme: 'dark' | 'light';
  sidebarCollapsed: boolean;
  activeMenu: string;
  setTheme: (theme: 'dark' | 'light') => void;
  toggleSidebar: () => void;
  setActiveMenu: (menu: string) => void;
}

export const useAppStore = create<AppState>((set) => ({
  theme: 'dark',
  sidebarCollapsed: false,
  activeMenu: 'dashboard',
  setTheme: (theme) => set({ theme }),
  toggleSidebar: () => set((state) => ({ sidebarCollapsed: !state.sidebarCollapsed })),
  setActiveMenu: (activeMenu) => set({ activeMenu }),
}));
```

#### 数据获取
```tsx
// 使用 React Query 管理服务器状态
import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';

const useMarketData = (symbol: string) => {
  return useQuery({
    queryKey: ['marketData', symbol],
    queryFn: () => fetchMarketData(symbol),
    refetchInterval: 5000, // 5秒自动刷新
    staleTime: 1000, // 1秒内数据不过期
  });
};
```

#### WebSocket 连接
```tsx
// 实时数据连接
import { useWebSocket } from '../hooks/useWebSocket';

const MarketDataProvider = ({ children }) => {
  const { data: marketData } = useWebSocket('ws://localhost:8080/market');

  return (
    <MarketDataContext.Provider value={marketData}>
      {children}
    </MarketDataContext.Provider>
  );
};
```

### 3. 性能优化建议

#### 代码分割
```tsx
// 路由级别代码分割
const Dashboard = lazy(() => import('../pages/Dashboard'));
const Market = lazy(() => import('../pages/Market'));
const Backtest = lazy(() => import('../pages/Backtest'));

// 组件级别代码分割
const HeavyChart = lazy(() => import('../components/HeavyChart'));

// 使用 Suspense
<Suspense fallback={<ChartSkeleton />}>
  <HeavyChart />
</Suspense>
```

#### 虚拟滚动
```tsx
// 大数据列表虚拟化
import { FixedSizeList as List } from 'react-window';

const VirtualizedTable = ({ data }) => {
  const Row = ({ index, style }) => (
    <div style={style}>
      <TableRow data={data[index]} />
    </div>
  );

  return (
    <List
      height={600}
      itemCount={data.length}
      itemSize={50}
    >
      {Row}
    </List>
  );
};
```

#### 图表优化
```tsx
// 使用 React.memo 优化图表渲染
const OptimizedChart = memo(({ data, config }) => {
  return (
    <LineChart data={data} {...config} />
  );
}, (prevProps, nextProps) => {
  return isEqual(prevProps.data, nextProps.data);
});
```

### 4. 可访问性 (A11y)

#### 键盘导航
```tsx
// 表格键盘导航
const Table = () => {
  const [focusedRow, setFocusedRow] = useState(0);

  const handleKeyDown = (e: KeyboardEvent) => {
    switch (e.key) {
      case 'ArrowDown':
        setFocusedRow(prev => Math.min(prev + 1, data.length - 1));
        break;
      case 'ArrowUp':
        setFocusedRow(prev => Math.max(prev - 1, 0));
        break;
      case 'Enter':
        handleRowSelect(data[focusedRow]);
        break;
    }
  };

  return (
    <table onKeyDown={handleKeyDown}>
      {/* 表格内容 */}
    </table>
  );
};
```

#### ARIA 属性
```tsx
// 为图表添加可访问性标签
<div
  role="img"
  aria-label="过去30天的收益曲线图，显示从-2%到+5%的波动"
>
  <LineChart data={returnData} />
</div>
```

---

## 📝 总结

本设计方案为 xTrading 打造了一个**专业、现代、高性能**的量化交易界面，具有以下特点：

### ✅ 设计亮点

1. **专业交易终端美学**
   - 深色主题，专业氛围
   - 高对比度数据展示
   - 金融级色彩体系

2. **现代化交互体验**
   - 流畅的动画过渡
   - 实时数据更新
   - 直观的信息层次

3. **完整的响应式设计**
   - 桌面端专业布局
   - 移动端友好适配
   - 跨平台一致性

4. **高性能实现**
   - 组件化架构
   - 代码分割优化
   - 虚拟滚动技术

5. **可访问性支持**
   - 键盘导航
   - 屏幕阅读器支持
   - ARIA 标签完整

### 🎯 核心价值

- **效率优先**: 快速获取关键信息，一目了然
- **美观专业**: 现代化设计语言，提升品牌形象
- **体验流畅**: 丝滑动画，零延迟响应
- **功能完整**: 覆盖量化交易全流程需求

这套设计方案将为 xTrading 用户提供**专业、美观、高效**的交易体验，助力用户在量化交易领域取得成功！

---

**文档版本**: 1.0
**创建日期**: 2026-01-17
**设计师**: Claude
**下一步**: 开始实现 UI 代码
